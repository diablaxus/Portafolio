services:
  - type: web
    name: portafolio-frank-berrio
    env: node
    region: oregon
    plan: free
    buildCommand: cd backend && npm install
    startCommand: cd backend && npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        sync: false
        # Esta variable debe configurarse manualmente en Render Dashboard
        # postgresql://postgres:solomayoresde18@db.dwqsatksemonzjewfplh.supabase.co:5432/postgres
